# Archivo: frontend/.env
# Esta variable le dice a nuestra aplicación React dónde encontrar el backend.
# El prefijo VITE_ es requerido por Vite para exponer la variable al código del frontend.

VITE_API_URL=http://localhost:5000
